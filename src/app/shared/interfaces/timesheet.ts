export interface selectedWorker {  
  givenName: string;
  surName: string;
  afm: string;
}

export interface selectedClient {  
  brandName: string;  
  afm: string;
}

export interface WorkType {
  value: string;
  viewValue: string;
}
